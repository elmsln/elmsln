#!/bin/bash
# This is the execution to do drush si commands for areas that need
# installed but can't be produced via the jobs directory
# include configuration settings
source ../../config/scripts/drush-create-site/config.cfg
