elmsln-install.sh

Run this to install elmsln on a system that’s already been set in motion to handle it. This means that all the pre-installation procedures have happened either manually via the INSTALL.txt file or using the install step through in the root directory here.

All users that plan to administer ELMSLN in some way should also run the preinstaller located in the users directory here in install.