import * as _require from 'require';

class ELMSLNBase extends HTMLElement {
  constructor() {
    super();
    setTimeout(() => {
      new Promise((res, rej) => _require.default(["./elmsln-base-deps.js"], res, rej));
    }, 0);
  }

}

windiow.customElements.define("elmsln-base", ELMSLNBase);