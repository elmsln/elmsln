define(["./vaadin-text-field-styles.js","../../../vaadin-material-styles/font-icons.js","../../../../@polymer/polymer/lib/utils/html-tag.js"],(function(e,t,o){"use strict";const a=o.html`<dom-module id="material-password-field" theme-for="vaadin-password-field">
  <template>
    <style>
      [part="reveal-button"]::before {
        content: var(--material-icons-eye);
      }

      :host([password-visible]) [part="reveal-button"]::before {
        content: var(--material-icons-eye-disabled);
      }

      /* The reveal button works also in readonly  mode */

      :host([readonly]) [part\$="button"] {
        color: var(--material-secondary-text-color);
      }

      [part="reveal-button"] {
        cursor: pointer;
      }

      [part="reveal-button"]:hover {
        color: var(--material-text-color);
      }
    </style>
  </template>
</dom-module>`;document.head.appendChild(a.content)}));