import{html as e,css as t,LitElement as i}from"../../../lit-element/lit-element.js";import{GradeBookStore as s}from"./grade-book-store.js";import{nothing as l}from"../../../lit-html/lit-html.js";import{autorun as r,toJS as o}from"../../../mobx/dist/mobx.esm.js";import"./letter-grade.js";class LetterGradePicker extends i{constructor(){super(),this.value=null,this.label=null,this.tooltip=!1,this.arrow=!1,this.input=!1,this.possible=0,this.score=0}static get properties(){return{value:{type:String},label:{type:String},tooltip:{type:Boolean},arrow:{type:Boolean},input:{type:Boolean},possible:{type:Number},score:{type:Number}}}static get styles(){return[t`
        :host {
          display: block;
          font-size: 18px;
        }
        letter-grade {
          display: inline-flex;
          filter: brightness(80%) saturate(50%);
        }
        .wrapper {
          display: flex;
        }
        span.prefix,
        .wrapper ::slotted(*) {
          margin-right: 10px;
          width: 90px;
          line-height: 38px;
          display: block;
          padding: 0;
          text-align: end;
          height: 38px;
        }
        button {
          background-color: transparent;
          padding: 0;
          margin: 0;
          border: none;
          border-radius: none;
          cursor: pointer;
        }
        button[active="active"] letter-grade,
        letter-grade[active="active"] {
          filter: brightness(90%) saturate(4);
          outline: 2px solid black;
          outline-offset: -2px;
        }
        button[data-selected-value] letter-grade {
          filter: brightness(100%) saturate(1);
          outline: 4px solid black;
          outline-offset: -4px;
        }
        .score-wrapper {
          display: flex;
          font-size: 28px;
          line-height: 36px;
          word-spacing: -4px;
        }
        simple-fields-field {
          width: 64px;
          margin: 0px -8px 0px 16px;
          --simple-fields-font-size: 28px;
        }
        simple-tooltip {
          --simple-tooltip-background: #000000;
          --simple-tooltip-opacity: 1;
          --simple-tooltip-text-color: #ffffff;
          --simple-tooltip-delay-in: 0;
          --simple-tooltip-border-radius: 0;
        }
      `]}render(){return e`
      <div class="wrapper">
        <slot name="prefix"><span class="prefix">${this.label}</span></slot>
        ${s.gradeScale.map(((t,i)=>e`
            <button
              ?data-selected-value="${t.letter==this.value}"
              letter="${t.letter}"
              id="btn${i}"
              @click="${this.clickScore}"
              @mousedown="${this.activeEventOn}"
              @mouseover="${this.activeEventOn}"
              @focusin="${this.activeEventOn}"
              @mouseout="${this.activeEventOff}"
              @focusout="${this.activeEventOff}"
            >
              <letter-grade
                interactive
                display-only
                letter="${t.letter}"
                mini
              ></letter-grade>
            </button>
            ${this.tooltip?e` <simple-tooltip
                  for="btn${i}"
                  position="top"
                  animation-delay="0"
                >
                  ${t.lowRange}% - ${t.highRange}%
                </simple-tooltip>`:l}
          `))}
        ${this.input?e`
              <span class="score-wrapper">
                <simple-fields-field
                  type="number"
                  part="simple-fields-field"
                  min="0"
                  value="${this.score}"
                  max="${this.possible}"
                  @value-changed="${this.scoreUpdated}"
                  maxlength="10"
                ></simple-fields-field>
                / ${this.possible}</span
              >
            `:l}
        <slot name="suffix"></slot>
        ${this.input?e``:l}
      </div>
    `}activeEventOn(e){e.target.setAttribute("active","active")}activeEventOff(e){e.target.removeAttribute("active")}clickScore(e){this.value=e.target.getAttribute("letter");const t=o(s.gradeScale.find((e=>this.value===e.letter)));this.score=Math.round(t.highRange/100*this.possible*100)/100}scoreUpdated(e){const t=Math.round(e.detail.value/this.possible*100*100)/100,i=o(s.gradeScale.find((e=>e.highRange>=t&&t>=e.lowRange)));this.value=i.letter}updated(e){e.forEach(((e,t)=>{"input"===t&&this[t]&&import("../../simple-fields/lib/simple-fields-field.js"),"tooltip"===t&&this[t]&&import("../../simple-tooltip/simple-tooltip.js"),["value","score"].includes(t)&&this.dispatchEvent(new CustomEvent(`${t}-changed`,{composed:!1,bubbles:!1,cancelable:!0,detail:this}))}))}static get tag(){return"letter-grade-picker"}}customElements.define(LetterGradePicker.tag,LetterGradePicker);export{LetterGradePicker};