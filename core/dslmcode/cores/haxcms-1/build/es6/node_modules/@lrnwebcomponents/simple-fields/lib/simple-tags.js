import{css as t,html as e,LitElement as a}from"../../../lit/index.js";import"./simple-tag.js";export class SimpleTags extends a{static get properties(){return{tags:{type:String},accentColor:{type:String,attribute:"accent-color"},autoAccentColor:{type:Boolean,attribute:"auto-accent-color"}}}constructor(){super(),this.tags="",this.autoAccentColor=!1,this.accentColor=null}render(){return e` <div class="tag-container">
      ${this.tags?this.tags.split(",").map((t=>e`
              <simple-tag
                ?auto-accent-color="${this.autoAccentColor}"
                value="${t.trim()}"
                accent-color="${this.accentColor}"
              ></simple-tag>
            `)):""}
    </div>`}static get tag(){return"simple-tags"}static get haxProperties(){return new URL(`./${this.tag}.haxProperties.json`,import.meta.url).href}static get styles(){return[t`
        :host {
          display: block;
        }
        .tag-container {
          display: flex;
          flex-wrap: wrap;
          justify-content: center;
        }
        simple-tag {
          margin: 2px 2px;
        }
      `]}}customElements.define(SimpleTags.tag,SimpleTags);