import{LitElement as t,html as e,css as a}from"../../../lit/index.js";class MapMenuBuilder extends t{static get styles(){return[a`
        :host {
          display: block;
        }
      `]}constructor(){super(),this.items=[],import("./map-menu-submenu.js"),import("./map-menu-item.js")}render(){return e`
      ${this.items?this.items.map((t=>e`
              ${t.children.length>0?e`
                    <map-menu-submenu
                      title="${t.title}"
                      id="${t.id}"
                      url="${t.slug}"
                      icon="${t.metadata&&t.metadata.icon?t.metadata.icon:""}"
                      open="${!(!t.metadata||!t.metadata.active)&&t.metadata.active}"
                      avatar-label="${t.metadata&&t.metadata.avatarLabel?t.metadata.avatarLabel:""}"
                      selected="${this.selected}"
                      .published="${t.metadata.published}"
                    >
                      <map-menu-builder
                        .items="${t.children}"
                        selected="${this.selected}"
                      ></map-menu-builder>
                    </map-menu-submenu>
                  `:e`
                    <map-menu-item
                      title="${t.title}"
                      id="${t.id}"
                      url="${t.slug}"
                      icon="${t.metadata&&t.metadata.icon?t.metadata.icon:""}"
                      track-icon="${t.metadata&&t.metadata.accessData&&t.metadata.accessData.trackIcon?t.metadata.accessData.trackIcon:""}"
                      active-path="${this.activePath}"
                      selected="${this.selected}"
                      ?published="${t.metadata.published}"
                      ?locked="${t.metadata.locked}"
                      status="${t.metadata.status}"
                    ></map-menu-item>
                  `}
            `)):""}
    `}static get tag(){return"map-menu-builder"}static get properties(){return{items:{type:Array},selected:{type:String}}}createRenderRoot(){return this}}window.customElements.define(MapMenuBuilder.tag,MapMenuBuilder);export{MapMenuBuilder};