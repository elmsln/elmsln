{
  "canScale": false,

  "canEditSource": true,
  "gizmo": {
    "title": "User action",
    "description": "Track user actions and send data to xAPI stores.",
    "icon": "icons:touch-app",
    "color": "green",
    "tags": ["Other", "developer", "dev", "xapi", "analytics", "user", "track"],
    "meta": {
      "author": "HAXTheWeb core team"
    }
  },
  "settings": {
    "configure": [
      {
        "attribute": "track",
        "title": "Event to track",
        "description": "User action that triggers this event.",
        "inputMethod": "select",
        "options": {
          "visibility": "Can see this",
          "keypress": "Press a key here",
          "click": "Click this",
          "hover": "Hover over this"
        },
        "required": true
      },
      {
        "attribute": "every",
        "title": "Track every time",
        "description": "By default, only the first occurrence is tracked.",
        "inputMethod": "boolean",
        "required": false
      }
    ],
    "advanced": []
  }
}
