/**
 * Copyright 2019 The Pennsylvania State University
 * @license Apache-2.0, see License.md for full text.
 */
import{LitElement as t,html as e,css as i}from"../../../../../lit/index.js";import"./site-query.js";class SiteRenderQuery extends t{static get styles(){return[i`
        :host {
          display: block;
        }
        .list {
          display: flex;
          flex-direction: column;
        }
        .grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
          gap: var(--site-render-query-grid-gap, 16px);
        }
        .item {
          display: block;
        }
      `]}static get tag(){return"site-render-query"}render(){return e`
      <site-query
        .result="${this.result}"
        @result-changed="${this.resultEvent}"
        .sort="${this.sort}"
        .conditions="${this.conditions}"
      ></site-query>
      <div id="list" class="${this.grid?"grid":"list"}">
        ${this.__items.map(t=>e`
            <div class="item" .item="${t}">
              <slot></slot>
            </div>
          `)}
      </div>
    `}resultEvent(t){this.result=[...t.detail.value]}static get properties(){return{conditions:{type:Object},sort:{type:Object},grid:{type:Boolean},result:{type:Array},__items:{type:Array}}}constructor(){super(),this.conditions={},this.sort={},this.grid=!1,this.result=[],this.__items=[]}updated(t){t.forEach((t,e)=>{"result"==e&&(this.dispatchEvent(new CustomEvent("result-changed",{detail:{value:this[e]}})),this.__items=[...this.result])})}}globalThis.customElements.define(SiteRenderQuery.tag,SiteRenderQuery);export{SiteRenderQuery};