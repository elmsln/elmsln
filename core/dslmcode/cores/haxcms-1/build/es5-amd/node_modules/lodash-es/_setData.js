define(["exports", "./_baseSetData.js", "./_shortOut.js"], function (_exports, _baseSetData, _shortOut) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _baseSetData = babelHelpers.interopRequireDefault(_baseSetData);
  _shortOut = babelHelpers.interopRequireDefault(_shortOut);

  /**
   * Sets metadata for `func`.
   *
   * **Note:** If this function becomes hot, i.e. is invoked a lot in a short
   * period of time, it will trip its breaker and transition to an identity
   * function to avoid garbage collection pauses in V8. See
   * [V8 issue 2070](https://bugs.chromium.org/p/v8/issues/detail?id=2070)
   * for more details.
   *
   * @private
   * @param {Function} func The function to associate metadata with.
   * @param {*} data The metadata.
   * @returns {Function} Returns `func`.
   */
  var setData = (0, _shortOut.default)(_baseSetData.default);
  var _default = setData;
  _exports.default = _default;
});