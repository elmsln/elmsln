define(["exports", "./_baseIsRegExp.js", "./_baseUnary.js", "./_nodeUtil.js"], function (_exports, _baseIsRegExp, _baseUnary, _nodeUtil) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _baseIsRegExp = babelHelpers.interopRequireDefault(_baseIsRegExp);
  _baseUnary = babelHelpers.interopRequireDefault(_baseUnary);
  _nodeUtil = babelHelpers.interopRequireDefault(_nodeUtil);

  /* Node.js helper references. */
  var nodeIsRegExp = _nodeUtil.default && _nodeUtil.default.isRegExp;
  /**
   * Checks if `value` is classified as a `RegExp` object.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Lang
   * @param {*} value The value to check.
   * @returns {boolean} Returns `true` if `value` is a regexp, else `false`.
   * @example
   *
   * _.isRegExp(/abc/);
   * // => true
   *
   * _.isRegExp('/abc/');
   * // => false
   */

  var isRegExp = nodeIsRegExp ? (0, _baseUnary.default)(nodeIsRegExp) : _baseIsRegExp.default;
  var _default = isRegExp;
  _exports.default = _default;
});