define(["exports", "./_arrayFilter.js", "./_baseFilter.js", "./_baseIteratee.js", "./isArray.js", "./negate.js"], function (_exports, _arrayFilter, _baseFilter, _baseIteratee, _isArray, _negate) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _arrayFilter = babelHelpers.interopRequireDefault(_arrayFilter);
  _baseFilter = babelHelpers.interopRequireDefault(_baseFilter);
  _baseIteratee = babelHelpers.interopRequireDefault(_baseIteratee);
  _isArray = babelHelpers.interopRequireDefault(_isArray);
  _negate = babelHelpers.interopRequireDefault(_negate);

  /**
   * The opposite of `_.filter`; this method returns the elements of `collection`
   * that `predicate` does **not** return truthy for.
   *
   * @static
   * @memberOf _
   * @since 0.1.0
   * @category Collection
   * @param {Array|Object} collection The collection to iterate over.
   * @param {Function} [predicate=_.identity] The function invoked per iteration.
   * @returns {Array} Returns the new filtered array.
   * @see _.filter
   * @example
   *
   * var users = [
   *   { 'user': 'barney', 'age': 36, 'active': false },
   *   { 'user': 'fred',   'age': 40, 'active': true }
   * ];
   *
   * _.reject(users, function(o) { return !o.active; });
   * // => objects for ['fred']
   *
   * // The `_.matches` iteratee shorthand.
   * _.reject(users, { 'age': 40, 'active': true });
   * // => objects for ['barney']
   *
   * // The `_.matchesProperty` iteratee shorthand.
   * _.reject(users, ['active', false]);
   * // => objects for ['fred']
   *
   * // The `_.property` iteratee shorthand.
   * _.reject(users, 'active');
   * // => objects for ['barney']
   */
  function reject(collection, predicate) {
    var func = (0, _isArray.default)(collection) ? _arrayFilter.default : _baseFilter.default;
    return func(collection, (0, _negate.default)((0, _baseIteratee.default)(predicate, 3)));
  }

  var _default = reject;
  _exports.default = _default;
});