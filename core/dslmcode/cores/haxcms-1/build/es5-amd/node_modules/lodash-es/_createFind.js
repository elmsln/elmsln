define(["exports", "./_baseIteratee.js", "./isArrayLike.js", "./keys.js"], function (_exports, _baseIteratee, _isArrayLike, _keys) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _baseIteratee = babelHelpers.interopRequireDefault(_baseIteratee);
  _isArrayLike = babelHelpers.interopRequireDefault(_isArrayLike);
  _keys = babelHelpers.interopRequireDefault(_keys);

  /**
   * Creates a `_.find` or `_.findLast` function.
   *
   * @private
   * @param {Function} findIndexFunc The function to find the collection index.
   * @returns {Function} Returns the new find function.
   */
  function createFind(findIndexFunc) {
    return function (collection, predicate, fromIndex) {
      var iterable = Object(collection);

      if (!(0, _isArrayLike.default)(collection)) {
        var iteratee = (0, _baseIteratee.default)(predicate, 3);
        collection = (0, _keys.default)(collection);

        predicate = function predicate(key) {
          return iteratee(iterable[key], key, iterable);
        };
      }

      var index = findIndexFunc(collection, predicate, fromIndex);
      return index > -1 ? iterable[iteratee ? collection[index] : index] : undefined;
    };
  }

  var _default = createFind;
  _exports.default = _default;
});