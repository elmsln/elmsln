define(["exports", "./_baseClamp.js", "./_baseToString.js", "./toInteger.js", "./toString.js"], function (_exports, _baseClamp, _baseToString, _toInteger, _toString) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _baseClamp = babelHelpers.interopRequireDefault(_baseClamp);
  _baseToString = babelHelpers.interopRequireDefault(_baseToString);
  _toInteger = babelHelpers.interopRequireDefault(_toInteger);
  _toString = babelHelpers.interopRequireDefault(_toString);

  /**
   * Checks if `string` starts with the given target string.
   *
   * @static
   * @memberOf _
   * @since 3.0.0
   * @category String
   * @param {string} [string=''] The string to inspect.
   * @param {string} [target] The string to search for.
   * @param {number} [position=0] The position to search from.
   * @returns {boolean} Returns `true` if `string` starts with `target`,
   *  else `false`.
   * @example
   *
   * _.startsWith('abc', 'a');
   * // => true
   *
   * _.startsWith('abc', 'b');
   * // => false
   *
   * _.startsWith('abc', 'b', 1);
   * // => true
   */
  function startsWith(string, target, position) {
    string = (0, _toString.default)(string);
    position = position == null ? 0 : (0, _baseClamp.default)((0, _toInteger.default)(position), 0, string.length);
    target = (0, _baseToString.default)(target);
    return string.slice(position, position + target.length) == target;
  }

  var _default = startsWith;
  _exports.default = _default;
});