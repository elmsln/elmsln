define(["exports", "./_arrayPush.js", "./_baseFlatten.js", "./_copyArray.js", "./isArray.js"], function (_exports, _arrayPush, _baseFlatten, _copyArray, _isArray) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _arrayPush = babelHelpers.interopRequireDefault(_arrayPush);
  _baseFlatten = babelHelpers.interopRequireDefault(_baseFlatten);
  _copyArray = babelHelpers.interopRequireDefault(_copyArray);
  _isArray = babelHelpers.interopRequireDefault(_isArray);

  /**
   * Creates a new array concatenating `array` with any additional arrays
   * and/or values.
   *
   * @static
   * @memberOf _
   * @since 4.0.0
   * @category Array
   * @param {Array} array The array to concatenate.
   * @param {...*} [values] The values to concatenate.
   * @returns {Array} Returns the new concatenated array.
   * @example
   *
   * var array = [1];
   * var other = _.concat(array, 2, [3], [[4]]);
   *
   * console.log(other);
   * // => [1, 2, 3, [4]]
   *
   * console.log(array);
   * // => [1]
   */
  function concat() {
    var length = arguments.length;

    if (!length) {
      return [];
    }

    var args = Array(length - 1),
        array = arguments[0],
        index = length;

    while (index--) {
      args[index - 1] = arguments[index];
    }

    return (0, _arrayPush.default)((0, _isArray.default)(array) ? (0, _copyArray.default)(array) : [array], (0, _baseFlatten.default)(args, 1));
  }

  var _default = concat;
  _exports.default = _default;
});