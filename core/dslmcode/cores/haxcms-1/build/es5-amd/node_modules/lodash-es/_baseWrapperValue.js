define(["exports", "./_LazyWrapper.js", "./_arrayPush.js", "./_arrayReduce.js"], function (_exports, _LazyWrapper, _arrayPush, _arrayReduce) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = void 0;
  _LazyWrapper = babelHelpers.interopRequireDefault(_LazyWrapper);
  _arrayPush = babelHelpers.interopRequireDefault(_arrayPush);
  _arrayReduce = babelHelpers.interopRequireDefault(_arrayReduce);

  /**
   * The base implementation of `wrapperValue` which returns the result of
   * performing a sequence of actions on the unwrapped `value`, where each
   * successive action is supplied the return value of the previous.
   *
   * @private
   * @param {*} value The unwrapped value.
   * @param {Array} actions Actions to perform to resolve the unwrapped value.
   * @returns {*} Returns the resolved value.
   */
  function baseWrapperValue(value, actions) {
    var result = value;

    if (babelHelpers.instanceof(result, _LazyWrapper.default)) {
      result = result.value();
    }

    return (0, _arrayReduce.default)(actions, function (result, action) {
      return action.func.apply(action.thisArg, (0, _arrayPush.default)([result], action.args));
    }, result);
  }

  var _default = baseWrapperValue;
  _exports.default = _default;
});