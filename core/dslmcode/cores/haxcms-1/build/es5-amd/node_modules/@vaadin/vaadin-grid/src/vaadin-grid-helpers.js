define(["exports"], function (_exports) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.GridHelper = void 0;

  /**
  @license
  Copyright (c) 2017 Vaadin Ltd.
  This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
  */

  /** @private */
  var GridHelper = /*#__PURE__*/function () {
    function VaadinGridHelper() {
      babelHelpers.classCallCheck(this, VaadinGridHelper);
    }

    babelHelpers.createClass(VaadinGridHelper, null, [{
      key: "_updateColumnOrders",
      value:
      /**
        * @param {Array<Object>} columns array of columns to be modified
        * @param {number} scope multiplier added to base order for each column
        * @param {number} baseOrder base number used for order
        */
      function _updateColumnOrders(columns, scope, baseOrder) {
        var c = 1;
        columns.forEach(function (column) {
          // avoid multiples of 10 because they introduce and extra zero and
          // causes the underlying calculations for child order goes wrong
          if (c % 10 === 0) {
            c++;
          }

          column._order = baseOrder + c * scope;
          c++;
        });
      }
    }]);
    return VaadinGridHelper;
  }();

  _exports.GridHelper = GridHelper;
});