define(["exports", "./to-int.js"], function (_exports, _toInt) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.default = compareArrays;
  _toInt = babelHelpers.interopRequireDefault(_toInt);

  // compare two arrays, return the number of differences
  function compareArrays(array1, array2, dontConvert) {
    var len = Math.min(array1.length, array2.length),
        lengthDiff = Math.abs(array1.length - array2.length),
        diffs = 0,
        i;

    for (i = 0; i < len; i++) {
      if (dontConvert && array1[i] !== array2[i] || !dontConvert && (0, _toInt.default)(array1[i]) !== (0, _toInt.default)(array2[i])) {
        diffs++;
      }
    }

    return diffs + lengthDiff;
  }
});