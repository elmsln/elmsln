define(["exports", "../utils/is-function.js"], function (_exports, _isFunction) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.calendar = calendar;
  _exports.defaultCalendar = void 0;
  _isFunction = babelHelpers.interopRequireDefault(_isFunction);
  var defaultCalendar = {
    sameDay: "[Today at] LT",
    nextDay: "[Tomorrow at] LT",
    nextWeek: "dddd [at] LT",
    lastDay: "[Yesterday at] LT",
    lastWeek: "[Last] dddd [at] LT",
    sameElse: "L"
  };
  _exports.defaultCalendar = defaultCalendar;

  function calendar(key, mom, now) {
    var output = this._calendar[key] || this._calendar["sameElse"];
    return (0, _isFunction.default)(output) ? output.call(mom, now) : output;
  }
});