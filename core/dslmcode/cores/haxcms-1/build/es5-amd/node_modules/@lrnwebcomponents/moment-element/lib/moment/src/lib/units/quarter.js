define(["exports", "../format/format.js", "./aliases.js", "./priorities.js", "../parse/regex.js", "../parse/token.js", "./constants.js", "../utils/to-int.js"], function (_exports, _format, _aliases, _priorities, _regex, _token, _constants, _toInt) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.getSetQuarter = getSetQuarter;
  _toInt = babelHelpers.interopRequireDefault(_toInt);
  // FORMATTING
  (0, _format.addFormatToken)("Q", 0, "Qo", "quarter"); // ALIASES

  (0, _aliases.addUnitAlias)("quarter", "Q"); // PRIORITY

  (0, _priorities.addUnitPriority)("quarter", 7); // PARSING

  (0, _regex.addRegexToken)("Q", _regex.match1);
  (0, _token.addParseToken)("Q", function (input, array) {
    array[_constants.MONTH] = ((0, _toInt.default)(input) - 1) * 3;
  }); // MOMENTS

  function getSetQuarter(input) {
    return input == null ? Math.ceil((this.month() + 1) / 3) : this.month((input - 1) * 3 + this.month() % 3);
  }
});