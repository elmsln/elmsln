function _createSuper(Derived) { var hasNativeReflectConstruct = _isNativeReflectConstruct(); return function _createSuperInternal() { var Super = babelHelpers.getPrototypeOf(Derived), result; if (hasNativeReflectConstruct) { var NewTarget = babelHelpers.getPrototypeOf(this).constructor; result = Reflect.construct(Super, arguments, NewTarget); } else { result = Super.apply(this, arguments); } return babelHelpers.possibleConstructorReturn(this, result); }; }

function _isNativeReflectConstruct() { if (typeof Reflect === "undefined" || !Reflect.construct) return false; if (Reflect.construct.sham) return false; if (typeof Proxy === "function") return true; try { Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); return true; } catch (e) { return false; } }

/*
`page-scroll-position`
A Web Component that hold the current scroll value relative to the entire document.
*/
var PageScrollPosition = /*#__PURE__*/function (_HTMLElement) {
  "use strict";

  babelHelpers.inherits(PageScrollPosition, _HTMLElement);

  var _super = _createSuper(PageScrollPosition);

  function PageScrollPosition() {
    babelHelpers.classCallCheck(this, PageScrollPosition);
    return _super.apply(this, arguments);
  }

  babelHelpers.createClass(PageScrollPosition, [{
    key: "attachedCallback",
    value: function attachedCallback() {
      var _this = this;

      // start off at 0
      this.value = 0.0;
      var element = document;
      var valueChangedEvent = new CustomEvent("value-changed", {
        detail: {
          value: 0.0
        }
      });
      this.dispatchEvent(valueChangedEvent);
      element.addEventListener("scroll", function () {
        // get the height to the top
        var a = document.documentElement.scrollTop; // get how far down the page they have scrolled

        var b = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        var c = a / b * 100; // set value to the percent of the way through

        _this.value = c;
        valueChangedEvent = new CustomEvent("value-changed", {
          detail: {
            value: _this.value
          }
        });

        _this.dispatchEvent(valueChangedEvent);
      });
    }
  }]);
  return PageScrollPosition;
}( /*#__PURE__*/babelHelpers.wrapNativeSuper(HTMLElement));

window.customElements.define("page-scroll-position", PageScrollPosition);