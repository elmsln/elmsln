define(["exports", "../../lit-element/lit-element.js", "../simple-picker/simple-picker.js", "../../@polymer/iron-meta/iron-meta.js"], function (_exports, _litElement, _simplePicker, _ironMeta) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.SimpleIconPicker = void 0;

  function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

  function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { babelHelpers.defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

  function _templateObject_866efd90accf11ea9f8e253ae0e7846d() {
    var data = babelHelpers.taggedTemplateLiteral(["\n        :host(simple-icon-picker) #collapse {\n          width: 300px;\n        }\n        :host(simple-icon-picker) .row {\n          justify-content: flex-start;\n        }\n        :host(simple-icon-picker) simple-picker-option {\n          flex: 0 0 auto;\n        }\n      "]);

    _templateObject_866efd90accf11ea9f8e253ae0e7846d = function _templateObject_866efd90accf11ea9f8e253ae0e7846d() {
      return data;
    };

    return data;
  }

  /**
   * `simple-icon-picker`
   * @element simple-icon-picker
   * Uses simple-picker to create an icon picker
   *
  
   * @demo ./demo/index.html
   */
  var SimpleIconPicker =
  /*#__PURE__*/
  function (_SimplePicker) {
    babelHelpers.inherits(SimpleIconPicker, _SimplePicker);
    babelHelpers.createClass(SimpleIconPicker, null, [{
      key: "styles",
      //styles function
      get: function get() {
        return [].concat(babelHelpers.toConsumableArray(babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker), "styles", this)), [(0, _litElement.css)(_templateObject_866efd90accf11ea9f8e253ae0e7846d())]);
      } // properties available to the custom element for data binding

    }, {
      key: "properties",
      get: function get() {
        return _objectSpread({}, babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker), "properties", this), {
          /**
           * Allow a null option to be selected?
           */
          allowNull: {
            name: "allowNull",
            type: Boolean
          },

          /**
            * An array of icons by name: ```
          [
          "editor:format-paint",
          "content-copy",
          "av:volume-off"
          
          ]```
          */
          icons: {
            name: "icons",
            type: Array
          },

          /**
           * The value of the option.
           */
          value: {
            name: "value",
            type: String,
            reflect: true
          },

          /**
           * the maximum number of options per row
           */
          optionsPerRow: {
            name: "optionsPerRow",
            type: Number
          },

          /**
            * An array of icons by name: ```
          [
          "editor:format-paint",
          "content-copy",
          "av:volume-off"
          
          ]```
          */
          __iconList: {
            name: "__iconList",
            type: Array
          }
        });
      }
      /**
       * Store the tag name to make it easier to obtain directly.
       * @notice function name must be here for tooling to operate correctly
       */

    }, {
      key: "tag",
      get: function get() {
        return "simple-icon-picker";
      }
    }]);

    function SimpleIconPicker() {
      var _this;

      babelHelpers.classCallCheck(this, SimpleIconPicker);
      _this = babelHelpers.possibleConstructorReturn(this, babelHelpers.getPrototypeOf(SimpleIconPicker).call(this));
      _this.hideOptionLabels = true;
      _this.allowNull = false;
      _this.icons = [];
      _this.value = null;
      _this.options = [];
      _this.optionsPerRow = 10;
      return _this;
    }
    /**
     * LitElement life cycle - property changed callback
     */


    babelHelpers.createClass(SimpleIconPicker, [{
      key: "updated",
      value: function updated(changedProperties) {
        var _this2 = this;

        if (babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker.prototype), "updated", this)) {
          babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker.prototype), "updated", this).call(this, changedProperties);
        }

        changedProperties.forEach(function (oldValue, propName) {
          if (["optionsPerRow", "icons", "allowNull", "__iconList"].includes(propName)) {
            _this2._getOptions(_this2[propName], oldValue);
          }

          if (propName == "value") {
            /**
             * fires when value changes
             * @event value-changed
             */
            _this2.dispatchEvent(new CustomEvent("value-changed", {
              detail: {
                value: _this2[propName]
              }
            }));
          }
        });
      }
      /**
       * LitElement life cycle - ready callback
       */

    }, {
      key: "firstUpdated",
      value: function firstUpdated(changedProperties) {
        if (babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker.prototype), "firstUpdated", this)) {
          babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker.prototype), "firstUpdated", this).call(this, changedProperties);
        }

        var iconSets = new _ironMeta.IronMeta({
          type: "iconset"
        });

        if (this.icons.length === 0 && babelHelpers.typeof(iconSets) !== (typeof undefined === "undefined" ? "undefined" : babelHelpers.typeof(undefined)) && iconSets.list && iconSets.list.length) {
          var iconList = [];
          iconSets.list.forEach(function (item) {
            item.getIconNames().forEach(function (icon) {
              iconList.push(icon);
            });
          });
          this.__iconList = iconList;

          this._setSelectedOption();
        }
      }
      /**
       * gets a list of icons and load them in a format
       * that the simple-picker can take;
       * if no icons are provided, loads a list from iron-meta
       *
       * @param {array} a list of custom icons for the picker
       * @param {array} default list of icons for the picker
       * @param {boolean} allow a null value for the picker
       *
       */

    }, {
      key: "_getOptions",
      value: function _getOptions() {
        var icons = typeof this.icons === "string" ? JSON.parse(this.icons) : this.icons,
            collapse = this.shadowRoot.querySelector("#collapse"),
            cols = this.optionsPerRow;
        if (icons.length === 0 && this.__iconList && this.__iconList.length > 0) icons = this.__iconList;
        var options = this.allowNull === false ? [] : [[{
          alt: "null",
          value: null
        }]],
            h = this.allowNull === false ? 0 : 1;
        cols = Math.sqrt(icons.length + h) <= this.optionsPerRow ? Math.ceil(Math.sqrt(icons.length + h)) : this.optionsPerRow;

        for (var i = 0; i < icons.length; i++) {
          var j = h + i,
              row = Math.floor(j / cols),
              col = j - row * cols;
          if (options[row] === undefined || options[row] === null) options[row] = [];
          options[row][col] = {
            alt: icons[i],
            icon: icons[i],
            value: icons[i]
          };
        }

        this.options = options;
      }
      /**
       * Don't set the selection option until there are options rendered
       */

    }, {
      key: "_setSelectedOption",
      value: function _setSelectedOption() {
        if (this.options.length > 1) babelHelpers.get(babelHelpers.getPrototypeOf(SimpleIconPicker.prototype), "_setSelectedOption", this).call(this);
      }
    }]);
    return SimpleIconPicker;
  }(_simplePicker.SimplePicker);

  _exports.SimpleIconPicker = SimpleIconPicker;
  window.customElements.define(SimpleIconPicker.tag, SimpleIconPicker);
});