define(["exports", "../format/format.js", "./aliases.js", "./priorities.js", "../parse/regex.js", "./year.js", "../create/date-from-array.js", "../parse/token.js", "../utils/to-int.js"], function (_exports, _format, _aliases, _priorities, _regex, _year, _dateFromArray, _token, _toInt) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.getSetDayOfYear = getSetDayOfYear;
  _toInt = babelHelpers.interopRequireDefault(_toInt);
  // FORMATTING
  (0, _format.addFormatToken)("DDD", ["DDDD", 3], "DDDo", "dayOfYear"); // ALIASES

  (0, _aliases.addUnitAlias)("dayOfYear", "DDD"); // PRIORITY

  (0, _priorities.addUnitPriority)("dayOfYear", 4); // PARSING

  (0, _regex.addRegexToken)("DDD", _regex.match1to3);
  (0, _regex.addRegexToken)("DDDD", _regex.match3);
  (0, _token.addParseToken)(["DDD", "DDDD"], function (input, array, config) {
    config._dayOfYear = (0, _toInt.default)(input);
  }); // HELPERS
  // MOMENTS

  function getSetDayOfYear(input) {
    var dayOfYear = Math.round((this.clone().startOf("day") - this.clone().startOf("year")) / 864e5) + 1;
    return input == null ? dayOfYear : this.add(input - dayOfYear, "d");
  }
});