define(["exports", "../utils/is-function.js"], function (_exports, _isFunction) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.defaultRelativeTime = void 0;
  _exports.pastFuture = pastFuture;
  _exports.relativeTime = relativeTime;
  _isFunction = babelHelpers.interopRequireDefault(_isFunction);
  var defaultRelativeTime = {
    future: "in %s",
    past: "%s ago",
    s: "a few seconds",
    ss: "%d seconds",
    m: "a minute",
    mm: "%d minutes",
    h: "an hour",
    hh: "%d hours",
    d: "a day",
    dd: "%d days",
    M: "a month",
    MM: "%d months",
    y: "a year",
    yy: "%d years"
  };
  _exports.defaultRelativeTime = defaultRelativeTime;

  function relativeTime(number, withoutSuffix, string, isFuture) {
    var output = this._relativeTime[string];
    return (0, _isFunction.default)(output) ? output(number, withoutSuffix, string, isFuture) : output.replace(/%d/i, number);
  }

  function pastFuture(diff, output) {
    var format = this._relativeTime[diff > 0 ? "future" : "past"];
    return (0, _isFunction.default)(format) ? format(output) : format.replace(/%s/i, output);
  }
});