define(["exports", "../units/aliases.js", "./from-array.js", "../utils/map.js"], function (_exports, _aliases, _fromArray, _map) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.configFromObject = configFromObject;
  _map = babelHelpers.interopRequireDefault(_map);

  function configFromObject(config) {
    if (config._d) {
      return;
    }

    var i = (0, _aliases.normalizeObjectUnits)(config._i);
    config._a = (0, _map.default)([i.year, i.month, i.day || i.date, i.hour, i.minute, i.second, i.millisecond], function (obj) {
      return obj && parseInt(obj, 10);
    });
    (0, _fromArray.configFromArray)(config);
  }
});