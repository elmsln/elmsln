define(["exports", "../duration/create.js", "../create/local.js", "./constructor.js"], function (_exports, _create, _local, _constructor) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.from = from;
  _exports.fromNow = fromNow;

  function from(time, withoutSuffix) {
    if (this.isValid() && ((0, _constructor.isMoment)(time) && time.isValid() || (0, _local.createLocal)(time).isValid())) {
      return (0, _create.createDuration)({
        to: this,
        from: time
      }).locale(this.locale()).humanize(!withoutSuffix);
    } else {
      return this.localeData().invalidDate();
    }
  }

  function fromNow(withoutSuffix) {
    return this.from((0, _local.createLocal)(), withoutSuffix);
  }
});