define(["./prototype.js", "./locales.js", "../utils/to-int.js"], function (_prototype, _locales, _toInt) {
  "use strict";

  _toInt = babelHelpers.interopRequireDefault(_toInt);
  (0, _locales.getSetGlobalLocale)("en", {
    dayOfMonthOrdinalParse: /\d{1,2}(th|st|nd|rd)/,
    ordinal: function ordinal(number) {
      var b = number % 10,
          output = (0, _toInt.default)(number % 100 / 10) === 1 ? "th" : b === 1 ? "st" : b === 2 ? "nd" : b === 3 ? "rd" : "th";
      return number + output;
    }
  });
});