define(["exports"], function (_exports) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.Random = void 0;
  _exports.randomSeed = randomSeed;

  function randomSeed() {
    return Math.floor(Math.random() * Math.pow(2, 31));
  }

  var Random = /*#__PURE__*/function () {
    function Random(seed) {
      babelHelpers.classCallCheck(this, Random);
      this.seed = seed;
    }

    babelHelpers.createClass(Random, [{
      key: "next",
      value: function next() {
        if (this.seed) {
          return (Math.pow(2, 31) - 1 & (this.seed = Math.imul(48271, this.seed))) / Math.pow(2, 31);
        } else {
          return Math.random();
        }
      }
    }]);
    return Random;
  }();

  _exports.Random = Random;
});