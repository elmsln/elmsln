define(["exports"], function (_exports) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.buildTemplate = void 0;
  var TEXT_MODE = 0;
  var TAGNAME_MODE = 1;
  var ATTRIBUTE_MODE = 2; // const ATTRIBUTE_VALUE_MODE = 3;
  // const attributeStartRegex = /([ \x09\x0a\x0c\x0d])([^\0-\x1F\x7F-\x9F
  // "'>=/]+)/;

  var buildTemplate = function buildTemplate(strings) {
    var l = strings.length - 1;
    var html = '';
    var mode = TEXT_MODE;

    for (var i = 0; i < l; i++) {
      var s = strings[i];

      for (var j = 0; j < s.length; j++) {
        var c = s[j];

        switch (mode) {
          case TEXT_MODE:
            if (c === '<') {
              mode = TAGNAME_MODE;
            }

            break;

          case TAGNAME_MODE:
            if (c === '!') {// maybe comment open
            } else if (c === ' ' || c === '\t' || c === '\n') {
              mode = ATTRIBUTE_MODE;
            }

            break;

          case ATTRIBUTE_MODE:
            // if
            console.log('attribute');
            break;
        }
      }
    }

    return html;
  };

  _exports.buildTemplate = buildTemplate;
});