/**
 * @file
 * Minor Jquery handling for rubric usability while viewing.
 */
(function ($) {
  Drupal.behaviors.rubricView = {
    attach: function(context) {
      //$('.field-name-field-rubric-levels .rubric_level_container').equalHeights();
    }
  };
})(jQuery);
