<?php

/**
 * Implements hook_install().
 */
function hax_install() {
  drupal_set_message(t('Make sure you copy bower.json from the hax module directory into sites/all/libraries/webcomponents/polymer/ and then run bower install to get dependencies in order for HAX to work.'), 'status');
}
