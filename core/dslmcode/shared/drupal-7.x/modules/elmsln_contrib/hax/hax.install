<?php

/**
 * Implements hook_install().
 */
function hax_install() {
  drupal_set_message(t('Make sure you run bower install in the hax directory to get dependencies in order for HAX to work.'), 'status');
}
