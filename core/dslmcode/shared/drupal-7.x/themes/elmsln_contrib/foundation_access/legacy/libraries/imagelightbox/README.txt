This is a fork of http://osvaldas.info/image-lightbox-responsive-touch-friendly jQuery plugin. The original plugin did not allow for images to have cache busting strings in the image path. This for allows you to include:

$( selector ).imageLightbox({
    allowedTypes:   'all',
});
